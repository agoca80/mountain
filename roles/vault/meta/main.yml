---
# vault/meta/main.yml

dependencies:  
  - role: consul
  - role: service
    service: vault
    packages:
      - vault
    rc:
      vault_syslog_output_enable: "YES"
      vault_user: vault
      vault_group: vault
